# SASS-Intro
A repository created while learning SCSS.

The index.html file have some basic HTML elements. The style.scss file have the SASS based styling for the HTML elements.
(The .scss file includes the basic SASS features such as Variables, Nesting, Partials/Modules, Mixins, Inheritance and Loops).
